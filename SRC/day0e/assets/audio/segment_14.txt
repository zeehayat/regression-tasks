Segment 14. Derive the OLS normal equations. Setting the squared-error gradient to zero yields the normal equations. The formula on screen is β̂=(XᵀX)⁻¹Xᵀy. Read it slowly and connect each symbol to a number. In our running microhydro power example, Use the book's MHP projects and million-PKR costs. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.