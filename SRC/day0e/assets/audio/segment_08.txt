Segment 8. Taylor expansion. Zooming in turns a smooth function into a local polynomial. The formula on screen is f(x+h)≈f(x)+f'(x)h+½f''(x)h². Read it slowly and connect each symbol to a number. In our running microhydro power example, A local cost approximation is useful only near the appraisal point. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.