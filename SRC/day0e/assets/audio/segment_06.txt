Segment 6. Sigmoid, tanh, and ReLU. Activation derivatives explain saturation and vanishing gradients. The formula on screen is σ'(x)=σ(x)(1−σ(x)). Read it slowly and connect each symbol to a number. In our running microhydro power example, A risk score for an MHP warning can become insensitive in a saturated tail. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.