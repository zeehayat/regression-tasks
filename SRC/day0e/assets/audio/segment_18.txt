Segment 18. Constrained optimisation and Lagrange multipliers. At a constrained optimum, objective and constraint gradients align. The formula on screen is ∇E=λ∇g. Read it slowly and connect each symbol to a number. In our running microhydro power example, Ridge and lasso are constrained least-squares ideas. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.