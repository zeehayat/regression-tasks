Segment 20. Symbolic differentiation with SymPy. Symbolic tools check reasoning; they do not replace it. The formula on screen is sympy.diff(E,m). Read it slowly and connect each symbol to a number. In our running microhydro power example, Confirm the symbolic MHP squared-error gradient. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.