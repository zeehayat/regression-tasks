Segment 23. Reading a traceback. The last line translates a failure into plain English. The formula on screen is symptom → cause → check. Read it slowly and connect each symbol to a number. In our running microhydro power example, Use shape, key, and type failures with MHP records. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.