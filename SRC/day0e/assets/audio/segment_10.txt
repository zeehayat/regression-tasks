Segment 10. The gradient and directional derivatives. The gradient points toward steepest ascent. The formula on screen is ∇E=[∂E/∂m,∂E/∂c]. Read it slowly and connect each symbol to a number. In our running microhydro power example, Negative gradient reduces the MHP cost error. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.