Segment 4. Product, quotient, and chain rules. Nested functions pass sensitivity through each layer. The formula on screen is d f(g(x))/dx = f'(g(x))g'(x). Read it slowly and connect each symbol to a number. In our running microhydro power example, The squared MHP cost error gives the same gradient by two roads. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.