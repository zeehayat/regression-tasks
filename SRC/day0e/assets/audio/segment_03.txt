Segment 3. The rules you will actually use. Power, constant-multiple, and sum rules are compressed limit calculations. The formula on screen is d(xⁿ)/dx = n xⁿ⁻¹. Read it slowly and connect each symbol to a number. In our running microhydro power example, A cost curve with cable length and terrain index is differentiated term by term. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.