Segment 5. Exponential and logarithm. Exponential growth reproduces itself; logarithms undo it. The formula on screen is d eˣ/dx=eˣ; d ln(x)/dx=1/x. Read it slowly and connect each symbol to a number. In our running microhydro power example, Log-transformed million-PKR costs change the derivative scale. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.