Segment 2. The limit definition, derived not asserted. A derivative is a limit of nearby average slopes. The formula on screen is f'(x) = lim h→0 [f(x+h)-f(x)]/h. Read it slowly and connect each symbol to a number. In our running microhydro power example, At a terrain index of 3, the numerical slope approaches 6. Now type the smallest version yourself; do not copy it. Pause the video now. When you resume, run the self-check and explain in one sentence what the result means. Break one line deliberately, read the last error line, translate it into plain English, and rebuild it. This is code as proof: a rule is not finished until a number confirms it.