# Animation source note

The environment used for this build does not have Manim Community Edition
installed. The masterclass therefore uses Pillow-rendered 1920×1080 frames with
the same locked palette and ffmpeg assembly. This is the one intentional
pipeline deviation; the mathematical content and captions remain reproducible
with the documented command.
